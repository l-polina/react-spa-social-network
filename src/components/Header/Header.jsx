import React from "react";
import classes from "./Header.module.css";

const Header = () => {
  return <header className={classes.header}>logo+search+drop-down list</header>;
};

export default Header;
